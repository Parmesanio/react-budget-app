import React from 'react';

const Calender = () => {
    return (
        <div className="calender">
            <select>
                <option></option>
            </select>
        </div>
    );
}

export default Calender;